function one () {
    console.log("one")
}
function two () {
    console.log("two")
}
function three () {
    console.log("three")
}

// sync = runs code line by line if something happens to the func two 
// the func three will not work untill func two is run

// async = every function run on his own with out depending on the other ones 